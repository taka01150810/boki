# 簿記3級 仕訳 一問一答 — 設計ドキュメント

## 概要

日商簿記3級の**仕訳問題**を1問ずつ解いて答え合わせできる学習アプリ。
取引文を読み、借方・貸方の勘定科目（プルダウン）と金額（入力）を埋めて「解答する」を押すと、自動採点して正誤と正解の仕訳を表示する。

## 技術スタック

| 項目 | 採用 |
|---|---|
| フレームワーク | Next.js 16（App Router） |
| 言語 | TypeScript |
| スタイリング | Tailwind CSS v4 |
| 状態・永続化 | React `useState` + `localStorage`（バックエンド／DBなし） |
| ビルド | Turbopack |

## 主要な設計判断（インタビューで確定）

| 項目 | 決定 | 理由 |
|---|---|---|
| 中核形式 | 仕訳問題（取引文 → 借方/貸方） | 手元の3級仕訳データをそのまま活かせる |
| 採点 | 自動採点（入力 → 「解答する」→ 正誤判定） | 学習効果が高い |
| 勘定科目入力 | 問題ごとの候補（6個）からプルダウン選択 | ネット試験に近く、実装もシンプル |
| 金額入力 | テキスト入力（数値） | — |
| 入力行数 | 借方・貸方とも固定3行、空欄行は無視 | 正解の行数を漏らさない／実装が単純 |
| 出題フロー | 1問ずつ連続出題（解答 → 正誤 → 次へ） | 一問一答のテンポ |
| 出題順 | 問1→問15の順番通り | シャッフルは将来拡張 |
| 永続化 | `localStorage`（正誤履歴・再開位置） | DB不要で復習の土台になる |

## 採点ロジック（[src/lib/grading.ts](../src/lib/grading.ts)）

- 借方・貸方それぞれを **「(科目, 金額) の集合」として順不同で比較**。
- 埋めた行（科目と金額の両方が有効）だけを対象にし、両側とも過不足なく一致すれば「正解」。
- 金額入力は正規化してから比較する（`parseAmount`）:
  - 全角数字 → 半角
  - カンマ・空白・`￥`・`円` を除去
  - 数字以外が残ればその行は無効扱い（`1,300` も `1300` も同一とみなす）

## ディレクトリ構成

```
src/
├── app/
│   ├── layout.tsx          # <html lang="ja">・メタデータ
│   └── page.tsx            # トップページ（JournalQuiz を配置）
├── components/
│   └── JournalQuiz.tsx     # 出題・入力・採点・結果表示（クライアントコンポーネント）
├── data/
│   ├── types.ts            # JournalQuestion / JournalLine などの型
│   └── journal-questions.ts# 3級 仕訳15問（docs のmarkdownから変換）
└── lib/
    ├── grading.ts          # 金額正規化・順不同採点・金額整形
    └── useProgress.ts      # localStorage への進捗保存フック
docs/
├── boki3-workbook-01.md    # 元の問題データ（出典付き）
└── DESIGN.md               # 本ドキュメント
```

## localStorage の保存内容

キー: `boki-progress-v1`

```jsonc
{
  "results": { "q1": true, "q2": false },  // questionId -> 直近の解答が正解か
  "currentIndex": 2                         // 再開用の現在インデックス
}
```

- 正答率 = `results` のうち true の数 / 解答済み問題数。
- 「リセット」ボタンで全消去。
- SSR とのハイドレーション不整合を避けるため、`localStorage` の読み込みは**マウント後**に行う。

## 開発コマンド

```bash
npm run dev     # 開発サーバー（http://localhost:3000）
npm run build   # 本番ビルド
npm run lint    # ESLint
npm start       # 本番サーバー
```

## 今後の拡張候補（未実装）

- 出題順のシャッフル
- 苦手問題（不正解のみ）を復習するモード
- 2級データの追加（`types.ts` の `Grade` 型を用意済み）
- 問題データの追加（問題集②以降）
- 全勘定科目マスタからの選択（難易度アップ）
